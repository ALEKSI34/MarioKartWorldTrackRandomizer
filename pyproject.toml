[project]
name = "mariokartworldtrackrandomizer"
version = "0.1.0"
description = "A CLI application for randomizing Mario Kart World tracks for tournaments"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["typer>=0.9.0"]
